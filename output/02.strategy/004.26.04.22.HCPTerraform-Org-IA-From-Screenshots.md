---
title: HCP Terraform Organization IA - ILM_Demo_Space
---
flowchart LR
    org["Organization\nILM_Demo_Space"]

    org --> manage
    org --> visibility
    org --> registry
    org --> settings

    subgraph manage["Manage"]
        direction TB
        projects["Projects\nProject catalog and create project"]
        stacks["Stacks\nFeature-gated, enable from settings"]
        workspaces["Workspaces\nSearch, filters, health and run status"]
    end

    subgraph visibility["Visibility"]
        direction TB
        explorer["Explorer\nObject visibility and navigation entry"]
        usage["Usage report\nPlan metrics, compliance, subscription, billable resources"]
    end

    subgraph registry["Registry"]
        direction TB
        privateLib["Private Library"]
        publicNs["Public Namespaces"]

        privateLib --> modules["Modules"]
        privateLib --> providersTab["Providers"]
        privateLib --> stackComponents["Stack component configurations"]

        privateLib --> publish["Publish"]
        privateLib --> designConfig["Design configuration"]
        privateLib --> testGen["Test generation toggle"]
        publicNs --> newNamespace["Create or claim namespace"]
    end

    subgraph settings["Organization Settings"]
        direction TB

        subgraph core["Core"]
            direction TB
            sGeneral["General\nOrg profile, execution mode, stack enablement"]
            sPlanBilling["Plan and Billing"]
            sTags["Tag Management\nReserved keys and workspace-only tags"]
            sTeams["Teams"]
            sUsers["Users"]
            sVarSets["Variable sets"]
            sHealth["Health assessments\nDrift detection and continuous validation"]
            sRuns["Runs\nNeeds attention, running, on hold"]
        end

        subgraph integrations["Integrations"]
            direction TB
            sCost["Cost estimation"]
            sPolicies["Policies"]
            sPolicySets["Policy sets"]
            sRunTasks["Run tasks"]
        end

        subgraph security["Security"]
            direction TB
            sApiTokens["API tokens\nOrg, team, user, audit, TTL"]
            sAgents["Agents\nAgent pools and tokens"]
            sAuditing["Auditing\nAudit streaming and audit trails"]
            sAuth["Authentication\nSession timeout and forced re-auth"]
            sIP["IP allowlists"]
            sSSH["SSH keys\nPrivate module access"]
            sSSO["SSO\nIdP configuration and test/enable"]
        end

        subgraph vcs["Version Control"]
            direction TB
            vcsGeneral["VCS General\nSpeculative plan status behavior"]
            vcsEvents["VCS Events\nProvider event log and filters"]
            vcsProviders["VCS Providers\nOAuth connections"]
        end
    end

    projects --> workspaces
    workspaces --> sRuns
    sRunTasks --> sRuns
    sPolicies --> sPolicySets
    sPolicySets --> workspaces
    sVarSets --> workspaces
    sTags --> projects
    sTags --> workspaces
    sCost --> usage
    sAgents --> workspaces
    sSSH --> privateLib
    sSSO --> sUsers
    sAuth --> sUsers
    sAuditing --> vcsEvents
    vcsProviders --> projects
    vcsProviders --> workspaces
    vcsEvents --> vcsProviders