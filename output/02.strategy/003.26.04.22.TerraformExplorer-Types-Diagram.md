---
title: Terraform Explorer Types IA
---
flowchart LR
    source["URL\n+/app/ILM_Demo_Space/explorer/types"] --> auth[s20]
    auth -->|"No"| unauthorized["Unauthorized shell\n+Error page returned in this environment"]
    auth -->|"Yes"| shell

    note["Diagram basis\n+Route semantics + Terraform app IA\n+Live page content was not accessible here"] -.-> shell

    subgraph shell["Terraform App Shell"]
        direction TB
        global["Global header\n+Org and app context"]
        nav["Primary navigation\n+Home / Workspaces / Projects / Registry / Explorer"]
        crumbs["Local path\n+Explorer -> Types"]
    end

    shell --> page
    global --> crumbs
    nav --> crumbs

    subgraph page["Explorer Types Index"]
        direction TB
        header["Page header\n+Types"]
        controls["Browse controls\n+Search / Filter / Sort"]
        listing["Type catalog\n+Collection of available types"]
        card["Type row or card\n+Name / classification / usage summary"]
        empty["No results state\n+No matching types"]
    end

    crumbs --> header
    header --> controls
    controls --> listing
    listing --> card
    controls --> empty

    listing --> detail["Type detail route\n+Open a selected type"]

    subgraph detailview["Selected Type View"]
        direction TB
        identity["Type identity\n+Name / schema / description"]
        relations["Relationships\n+Connected objects or consumers"]
        instances["Instances or usages\n+Where this type appears"]
        actions["Follow-up actions\n+Inspect / trace / navigate"]
    end

    detail --> identity
    identity --> relations
    identity --> instances
    relations --> actions
    instances --> actions